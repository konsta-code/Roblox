-- TribesWorld.server.lua  [STILLGELEGT / RETIRED]
--
-- Die frueher hier fest verdrahtete Einzelmap-Generierung wurde durch den
-- datengetriebenen Map-Pool ersetzt:
--   * MapPoolConstants  -- Themes (Gras/Schnee/Wueste) + Map-Defs (Layout/Seed)
--   * WorldGen          -- parametrischer, optimierter Terrain-Generator
--   * MapDirector       -- waehlt eine Map, baut sie, seatet die Basen, Optik
--
-- Dieses Script ist in default.project.json nicht mehr registriert und tut
-- nichts. Datei bleibt nur als Wegweiser erhalten.

return
